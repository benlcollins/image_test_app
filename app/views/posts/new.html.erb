<h1>Add new post with image!</h1>

<%= form_for(@post, multipart: true) do |f| %>
	<p>
    	<%= f.label :title %>
    	<%= f.text_field :title %>
  	</p>
  	<p>
    	<%= f.file_field :image %>
  	</p>

  	<%= f.submit %>
<% end %>